// Enumeration for Loading Status
const LayoutConstants = {
  sidebarWidth: 220,
  betslipWidth: 360,
  splitPaneStyle: {
    top: '0px',
    position: 'fixed',
  },
};

export default LayoutConstants;
